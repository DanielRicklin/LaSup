<% if (err) { %>
<h2>Erreur 404 🤷‍♂️🤷‍♂</h2>
<% } else { %>
    <div id="modal1" class="modal">
        <div class="modal-content">
            <h4>Les interfaces</h4>
            <form action="" id="modalInterface"></form>
        </div>
        <div class="modal-footer">
            <button class="modal-close waves-effect waves-green btn-flat">Valider</button>
        </div>
    </div>
    <div class="row" style="padding: 20px 0px;">
        <form class="col s12">
          <div class="row">
            <div class="col s12 m6 l6">
                <div class="input-field col s12" style="display: none;">
                    <input value="<%= equipment._id %>" id="id" type="text" name="id">
                </div>
            <div class="input-field col s12">
                <input value="<%= equipment.name %>" id="name" type="text" class="validate" name="name">
                <label for="name">Nom</label>
            </div>
            <div class="input-field col s12">
                <input value="<%= equipment.ip %>" id="ip" type="text" class="validate" name="ip">
                <label for="ip">Adresse IP</label>
            </div>
            </div>
            <div class="col s6">
              <div class="input-field col s12">
                <textarea id="description" class="materialize-textarea" name="description"><%= equipment.description ? equipment.description : '' %></textarea>
                <label for="description">Description</label>
              </div>
            </div>
          </div>
        </form>
        <button class="waves-effect waves-light btn teal darken-3" id="addInterface" type="button">Ajouter des interfaces en supervisions</button>
    </div>
    <div class="row">
        <div class="col s12 m12 l6">
            <!-- <canvas id="myChart" width="395px" height="256px"></canvas> -->
            <h2 class="center">Pertes</h2>
            <div id="pertes"></div>
        </div>
        <div class="col s12 m12 l6">
            <!-- <canvas id="myChart" width="395px" height="256px"></canvas> -->
            <h2 class="center">Latences</h2>
            <div id="latences"></div>
        </div>
    </div>
    <style>
        #pertes, #latences {
          width: 100%;
          height: 400px;
        }
        
        </style>
<% } %>